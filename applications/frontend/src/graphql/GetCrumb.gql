query GetCrumb($id: ID!) {
    getCrumb(id: $id) {
        id
        title
        text
        type
        source
        votes
        createdAt
        authorId
        tags {
            key
            value
        }
    }
}
